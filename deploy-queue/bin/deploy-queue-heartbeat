#!/usr/bin/env bash
set -euo pipefail

if [ -z "${DEPLOY_QUEUE_HEARTBEAT_URL:-}" ]; then
  echo "DEPLOY_QUEUE_HEARTBEAT_URL is not set" >&2
  exit 1
fi

exec deploy-queue heartbeat url --url "${DEPLOY_QUEUE_HEARTBEAT_URL}"
